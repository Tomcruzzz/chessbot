# Copy this file to `.env` and fill in your Lichess BOT API token.
# Create a token at: https://lichess.org/account/oauth/token
# The token needs the "Play games with the bot API" (bot:play) scope.
LICHESS_TOKEN=your_token_here
